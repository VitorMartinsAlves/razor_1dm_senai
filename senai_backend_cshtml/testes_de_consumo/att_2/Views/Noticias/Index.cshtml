@Model
@{
        ViewData["Title"] = "Noticias";
    }

<div class="titulo_pagina">
<h1>Cadastro de @ViewData["Title"]</h1>
</div>

    <form  class="cadastro" method="POST" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">
 <div class="campo">
    <label>ID</label>
    <input type="text" name="IdNoticia" />
 </div>
   
<div class="campo">
    <label>Titulo</label>
    <input type="text" name="Titulo" />
</div>

<div class="campo">
    <label>Texto</label>
    <input type="text" name="Texto" />
</div>
    
<div class="campo">
    <label>Imagem</label>
    <input type="file" name="Imagem" />
</div>
    

    <button class="gradient btn" type="submit">Cadastrar</button>
</form>

@* <table class="table table-striped table-responsive">
    <thead>
        <th>ID</th>
        <th>Nome</th>
        <th>Foto</th>
    </thead>
    <tbody>
        @foreach(Models.Noticias e in ViewBag.Equipes){
            <tr>
                <td>@e.IdNoticia</td>
                <td>@e.Titulo</td>
                <td>@e.Texto</td>
                <td><img src="img/Equipes/@e.Imagem" alt="Imagem da equipe @e.Titulo" width="40"></td>
                <td><a asp-area="" asp-controller="Equipe" asp-action="Excluir" asp-route-id="@e.IdNoticia"><i class="fa fa-trash"></a></td>
            </tr>
        }
    </tbody>
</table> *@



 <div class="container content">
<section class="noticias">
@foreach(var data in ViewBag.Noticias){
        
            <div class="card">
                    <img src="img/Noticias/@data.Imagem" alt="Notícia sobre @data.Titulo">
                    <div>
                        <h4>@data.Titulo</h4>
                        <p> @data.Texto</p>
                    </div>
                    <a asp-area="" asp-controller="Noticias" asp-action="Excluir" asp-route-id="@data.IdNoticia"><i class="fa fa-trash"></i></a>

            </div>
}

</section>
</div> 